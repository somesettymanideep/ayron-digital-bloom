RewriteEngine On

# Force HTTPS and remove www in one step
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^www\.ayrondigitalsolutions\.com [NC]
RewriteRule ^(.*)$ https://ayrondigitalsolutions.com/$1 [L,R=301]

# Remove index.php (if exists in URL)
RewriteCond %{THE_REQUEST} /index\.php [NC]
RewriteRule ^(.*)index\.php$ /$1 [R=301,L]

# Optional: Remove trailing slash (except folders)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} (.+)/$
RewriteRule ^ %1 [R=301,L]

# SPA fallback - serve index.html for all routes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ /index.html [L]
